{
  "name": "linebot-booking-golf",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm install",
    "build": "cd client && npm run build && rm -rf ../dist && mv dist ../dist",
    "dev": "cd client && npm run dev"
  },
  "keywords": [
    "line-bot",
    "booking",
    "golf"
  ],
  "author": "",
  "license": "ISC",
  "description": "LINE Bot for golf booking",
  "dependencies": {
    "@line/bot-sdk": "^10.5.0",
    "concurrently": "^9.2.1",
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  }
}